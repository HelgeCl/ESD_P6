from Git.ESD_P6.Comm_Therk.TX import transmit
from Git.ESD_P6.Comm_Therk.TX_RX import RXTX

radio = RXTX()

radio.transmit("REQ:ACTIVE?")
transmit("REQ:ACTIVE?")  # Test message, can be changed to anything you'd like
